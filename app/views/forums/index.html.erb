<h1> "Forums" </h1>
 
<table>
  <tr>
    <th width="col-md-8">Forum</th>
    <th width="col-md-4">Last Post</th>
  </tr>
  <% for forum in @forums %>
    <tr>
      <td><h4><%= link_to h(forum.name), forum_path(forum.id) %></h4>
        <small><%= forum.topics.count %> topics</small><br />
        <%= forum.description %></td>
      <td class="right"></td>
      <td><%= link_to "Show", forum %></td>
      <td><%= link_to "Edit", edit_forum_path(forum) %></td>
      <td><%= link_to "Destroy", forum, :confirm => 'Yeah?', :method => :delete %></td>
    </tr>
  <% end %>
</table>
 
<p><%= link_to "New Forum", new_forum_path %></p>